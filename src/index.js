import React from 'react';
import ReactDOM from 'react-dom';

import { WebStoreApp } from './WebStoreApp';
import 'bootstrap/dist/js/bootstrap.bundle.min';

ReactDOM.render(
   <WebStoreApp />,
  document.getElementById('root')
);

